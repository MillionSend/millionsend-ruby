# frozen_string_literal: true

# build/release tasks (rubygems/release-gem drives `rake release`)
require "bundler/gem_tasks"
require "rspec/core/rake_task"

RSpec::Core::RakeTask.new(:spec)

task default: :spec
